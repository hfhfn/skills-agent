# Skills Agent 环境变量配置
# 复制此文件为 .env 并填入你的配置


# ============ 通用配置（推荐，适用于所有 Provider） ============

# 模型 Provider（如 openai、anthropic）
# 未设置时由 LangChain 根据模型名自动推断
# MODEL_PROVIDER=openai

# 通用 API Key（优先级高于 ANTHROPIC_* 系列变量）
# API_KEY=your-api-key

# 通用 API Base URL（用于 OpenAI 兼容接口，如智谱AI）
# API_BASE_URL=https://open.bigmodel.cn/api/paas/v4


# ============ Anthropic 专属配置 ============

# AUTH_TOKEN (api key)
ANTHROPIC_AUTH_TOKEN=sk_xxxx

# 自定义 API Base URL（用于代理或自定义端点）
# ANTHROPIC_BASE_URL=https://your-proxy-url


# ============ 模型配置 ============

# 默认使用的模型（可选值: claude-sonnet-4-20250514, claude-opus-4-20250514, glm-4.7-flash 等）
CLAUDE_MODEL=claude-opus-4-5-20251101

# 是否启用流式输出（默认 true，思考模型推荐开启以实时显示推理过程）
# 设为 false 可强制使用非流式同步调用
# ENABLE_STREAMING=true

# 最大对话轮数限制
MAX_TURNS=20

# 权限模式 (default, acceptEdits, bypassPermissions)
PERMISSION_MODE=bypassPermissions


# 使用openai兼容接口
MODEL_PROVIDER=openai
# ============ 智谱AI 配置示例 ============
CLAUDE_MODEL=glm-4.7-flash
API_KEY=your-zhipu-api-key
API_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# deepseek API
# CLAUDE_MODEL=deepseek-reasoner  # 非思考模式: deepseek-chat
# API_KEY=your-deepseek-api-key
# API_BASE_URL=https://api.deepseek.com
