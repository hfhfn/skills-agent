# =============================================================================
# .gitignore — Skills Agent 项目 Git 忽略规则
# =============================================================================

# ── 环境变量文件 ─────────────────────────────────────────────────────────────
# 包含 API Key、密钥等敏感信息，绝不应提交到仓库
.env                    # 默认环境变量文件
.env.local              # 本地覆盖配置
.env.*.local            # 带环境名的本地覆盖（如 .env.production.local）
# 注意：.env.example 不会被忽略，可以作为模板提交到仓库

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/            # Python 字节码缓存目录
*.py[cod]               # .pyc / .pyo / .pyd 编译文件
*.egg-info/             # pip 包元数据目录（开发模式安装时生成）
*.egg                   # Python egg 打包文件
dist/                   # 构建产物输出目录（python -m build）
build/                  # 构建临时目录

# ── uv (Python 包管理器) ────────────────────────────────────────────────────
.venv/                  # 虚拟环境目录（uv sync 自动创建）
uv.lock                 # uv 锁文件

# ── IDE / 编辑器 ─────────────────────────────────────────────────────────────
.idea/                  # JetBrains (PyCharm / IntelliJ) 项目配置
.vscode/                # VS Code 工作区配置
*.swp                   # Vim 交换文件（编辑中自动生成）
*.swo                   # Vim 交换文件（多重编辑时生成）
*~                      # Emacs / 部分编辑器的备份文件

# ── 输出目录 ─────────────────────────────────────────────────────────────────
# Skill 脚本（如 news-extractor）默认将结果写入 ./output/
output/

# ── Claude Code ──────────────────────────────────────────────────────────────
# 本地个人设置（如允许的工具列表），每人不同，不应提交
.claude/settings.local.json

# ── Node.js ──────────────────────────────────────────────────────────────────
# 前端 web/ 目录下有独立的 .gitignore，这里做根级兜底
node_modules/

# ── 操作系统生成文件 ─────────────────────────────────────────────────────────
.DS_Store               # macOS Finder 目录元数据
Thumbs.db               # Windows 缩略图缓存
Desktop.ini             # Windows 文件夹显示设置

# ── Windows 特殊设备文件名 ───────────────────────────────────────────────────
# Windows 上 "nul" 是保留设备名，防止误操作创建同名文件
nul
